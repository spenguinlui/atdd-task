# ATDD Hub 環境變數 - 複製為 .env 並修改
# cp .env.example .env
# 專案路徑統一由 .claude/config/projects.yml 管理

# 測試帳號（E2E 測試用）
TEST_USER_EMAIL=admin@example.com
TEST_USER_PASSWORD=your_test_password

# 專案路徑
CORE_WEB_PATH=/path/to/your/core_web

# AWS 基礎設施
AWS_SF_INSTANCE_ID=i-0your_instance_id

# ATDD 遠端 server API key（.mcp.json 以 ${ATDD_SERVER_API_KEY} 展開讀取）
# 請向管理者索取，勿硬編進任何 tracked 檔案
ATDD_SERVER_API_KEY=your_atdd_server_api_key
