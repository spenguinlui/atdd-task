你是資深規格分析師。針對下面這份需求，**只輸出規格文件本身**（不要使用任何工具、不要讀寫檔案、不要問問題），完整產出以下六段：

1. **信心度**：給一個百分比（0–100%），並逐維度（需求清晰度 / 邊界明確度 / 既有機制理解 / 資料粒度 / 風險可控 / 驗收可行 / 影響範圍）各一句理由。
2. **SA（系統分析）**：涉及的資料、資料流、改動範圍、Call Chain（進入點 → 目標）、風險點。
3. **Risk Pre-mortem**：想像「上線一週後讓你後悔的事故」反推，至少 3 條、橫跨至少 3 類（technical / domain / data / integration / ux），每條含 id、category、description、likelihood(low|medium|high)、impact(low|medium|high)、mitigation（對應到具體 scenario 或檢查，不得只寫「會注意」）。
4. **BA 報告**：**純中文、禁任何技術術語 / 英文技術詞 / 程式碼 / 底線命名**，必含三段標題：`## 需求摘要`、`## 業務分析結論`、`## 驗收條件`。
5. **Acceptance Criteria**：AC1…ACn 列表。
6. **Scenarios**：每個標 `S{n}-{happy|alt|error|edge|regression}`，用 Given-When-Then（或 假設-當-那麼）完整展開，不只寫標題。

---

## 需求

每月台電電費代收對帳。系統每月收到台電匯出的對帳檔（欄位：客戶編號、應收金額、計費期間起、計費期間迄）。需與系統內各客戶的應收帳款比對：

- 金額一致 → 標記「已對帳」。
- 差額絕對值 > 1 元 → 列入「例外清單」供財務複核。
- 對帳檔內有、但系統查無的客戶編號 → 列為「孤兒紀錄」。
- 對帳結果需可重跑：同一月份重複匯入，不得重複計列。
