---
description: 部署 atdd-task 到遠端 Server（via AWS SSM）
---

# Deploy

直接執行 bash script：

```bash
./infrastructure/deploy.sh
```

如果需要指定分支：

```bash
./infrastructure/deploy.sh <branch_name>
```

預設部署 `master` 分支。
